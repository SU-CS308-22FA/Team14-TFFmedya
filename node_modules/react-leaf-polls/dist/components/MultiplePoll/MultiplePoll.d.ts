/// <reference types="react" />
import type { Result } from '../../types/result';
import type { Theme } from '../../types/theme';
interface MultiplePollProps {
    question?: string;
    results: Result[];
    theme?: Theme;
    isVoted?: boolean;
    isVotedId?: number;
    onVote?(item: Result, results: Result[]): void;
}
declare const MultiplePoll: ({ question, results, theme, onVote, isVoted, isVotedId, }: MultiplePollProps) => JSX.Element;
export { MultiplePoll, MultiplePollProps };
